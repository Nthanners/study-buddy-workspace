# Don't `pip install -r requirements.txt` here — RVC's deps need to be
# installed in a specific order (fairseq fork before rvc-python --no-deps),
# otherwise pip fights with omegaconf==2.0.6 / faiss-cpu==1.7.3 pins.
#
# Use install.bat instead (Windows) or read it as a recipe for macOS/Linux.
#
# Reference list of what install.bat installs (do not pip-install this file directly):
#   torch==2.1.2
#   torchaudio==2.1.2
#   git+https://github.com/IAHispano/fairseq.git
#   numpy==1.23.5
#   scipy==1.11.4
#   librosa==0.10.1
#   pyworld
#   praat-parselmouth
#   soundfile
#   tqdm
#   scikit-learn
#   onnxruntime
#   faiss-cpu==1.7.3
#   tensorboardX
#   matplotlib
#   resampy
#   joblib
#   audioread
#   pydub
#   ffmpeg-python
#   ffmpy
#   local-attention
#   einops
#   pyngrok
#   rvc-python  (with --no-deps)
#   flask
#   flask-cors
#   edge-tts
