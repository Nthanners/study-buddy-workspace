{
  "buildCommand": "cd amai-login && npm install && npm run build",
  "outputDirectory": "amai-login/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
